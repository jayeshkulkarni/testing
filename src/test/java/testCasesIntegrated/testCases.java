package testCasesIntegrated;

import org.testng.annotations.Test;

public class testCases {
  @Test
  public void executeTC() throws InterruptedException 
  {
	 /* GammaLogin.selectBrowser();
	  GammaLogin.login();
	  Project.count_project();
	  GammaLogout.logout();
	  GammaLogout.close_browser(); */
  }
}